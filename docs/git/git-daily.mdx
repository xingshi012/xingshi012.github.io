---
sidebar_position: 1
---

# git 日常总结

在 IDE 中使用 `git` 时，会遇到线上仓库有 但是本地仓库没有某一个分支的情况，这时候需要执行 `git fetch`  命令 或者 GUI 界面的 `fetch` 去更新代码

在 IDEA 中 执行 `revert` 时，可能会遇到本地回退了代码，但是由于和线上关联了，会无法把代码同步到线上，所以需要执行 `git push -f` 强制推送代码到线上